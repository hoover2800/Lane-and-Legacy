# Lane & Legacy Publishing website

Static website for automatic deployment through GitHub → Cloudflare.

## Routine product updates
Edit `products.json`. Cloudflare will redeploy automatically after the change is committed to `main`.

## Before launch
Replace `#` placeholders with live Etsy/Amazon URLs and add social profile URLs.
